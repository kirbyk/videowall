<div class="field">
  <%= f.label :name %><br>
  <%= f.text_field :name %>
  <%= link_to_remove_fields "remove panel data", f %>

  <%= f.fields_for :texts do |builder| %>
    <%= render 'text_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields 'Add Text', f, :texts %>
</div>
